-To have full points for the assignments part u need to do at least 2 challenges for each category (including those solved in class)
-Points of the challanges goes down for each person solved it, it means nothing else.


-(shell command) 	file name	: reads file format and prints it out.
	-64 or 32 bits
	-dynamically linked: external library loaded from the bynery (libc, I/O,...)
	-not stripped: we have the name of the functions in the binary

-when passing from a binary to a developer friendly language, the resoult is not the original program, several thing go lost during the conversions.



-ipython opens python in shell
